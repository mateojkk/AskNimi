{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build -w asknim-web && npx esbuild serverless/entry.ts --bundle --platform=node --format=esm --outfile=api/[[...route]].js --log-level=error",
  "outputDirectory": "apps/web/dist",
  "functions": {
    "api/**": {
      "maxDuration": 60
    }
  }
}
